# 看板今日预测直白化实施计划

1. 在 `DashboardCommandServiceImplTest` 使用截图同组数据断言完整自然语言，并增加投研黑话禁用词检查。
2. 调整 `DashboardCommandServiceImpl` 的总判断、方向依据和开盘后观察条件，保留原有方向筛选和数据时效判断。
3. 将前端的盘中条件标题改成“开盘后看什么”，运行看板布局测试。
4. 运行相关后端测试、前端完整测试、生产构建和 `git diff --check`。
5. 审查最终文案并记录真实页面验证边界。
