# 看板正号字形对齐设计

## 目标

赚钱效应和主线题材中的正涨幅保持完整百分比文本，同时让正号与数字在视觉上垂直居中。

## 方案

将符号和数值拆为相邻的内联元素，百分比整体仍按同一基线排版。只对正号使用 `-0.1em` 的相对位移，分别适配赚钱效应的 20px 数值和题材标签的 12px 数值。负号、百分号、数字间距及响应式布局保持不变。

## 验收

桌面和移动布局均使用同一符号规则；正号独立校正，数值继续使用等宽数字字形，完整百分比仍作为一个连续展示单元。
