# 盘后总结游资席位同步设计

## 问题

盘后报告只读取 `market_opinion` 的 `ACTIVE_SEAT` 记录，但该数据此前只有手动同步入口；报告定时生成时数据库为空，就会缓存 0 条席位两天。

## 方案

生成报告时先按目标交易日读取活跃席位。若为空，调用现有市场观点同步服务，再按相同日期窗口重读一次；同步异常只降级为无席位证据，不影响其他盘后数据。同步升级盘后报告缓存版本，避免继续命中旧的空报告。

## 验收

已有活跃席位时不触发同步；缺失时最多补刷一次并重新读取；同步失败仍能生成报告并标记数据缺口。
