# 全屏与常用设置设计

## 目标

为全局工作台增加浏览器全屏能力，并将“设置”从普通工具菜单中独立出来。桌面端使用右上角图标入口，移动端固定在功能抽屉底部，保持现有底部导航和滑动抽屉不变。

## 设置内容

- 全屏模式：使用标准 Fullscreen API，并监听浏览器全屏状态变化。
- 减少动效：本地持久化，在文档根节点应用统一降动效样式。
- 最近搜索：显示本地记录数量并支持清除。
- 常用入口：复用现有系统参数和同步中心页面。

## 边界

界面偏好只保存在当前浏览器，不新增后端配置。浏览器不支持 Fullscreen API 时禁用开关并明确显示不可用。
