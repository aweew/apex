# 股票标识统一实施计划

1. 增加股票标识结构契约测试，覆盖公共组件、全局注册和主要页面复用。
2. 实现 `StockIdentity.vue`，复用 `SecurityMarketBadge.vue` 的市场识别与配色。
3. 将分离的代码/名称表格列合并为股票列，并替换卡片、搜索、详情标题中的重复结构。
4. 运行前端全量测试和生产构建。
5. 启动本地服务，在桌面与移动视口检查名称、标签、代码顺序、截断和点击状态。
