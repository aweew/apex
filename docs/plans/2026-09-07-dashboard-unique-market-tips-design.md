# 看板市场提示唯一化设计

## 问题

市场简报构建时会生成一条当前立场提示，实时行情刷新后会重新计算立场并写入新的提示。均衡立场的旧提示未被替换，导致同一条内容重复显示。

## 方案

实时行情重算立场时，先从提示列表中移除上一轮生成的立场提示，再把当前立场提示放到首位。读取内存、Redis 或数据库快照时，再按提示正文保留第一条有效内容，使已有缓存无需等待重建即可恢复正常展示。行情、评分、仓位建议和其他不同内容的市场提示保持不变。

## 验收

同一个简报对象连续刷新多次，或缓存中已经存在相同内容时，返回的当前立场提示只保留一条，其他不同内容的市场提示仍正常展示。
