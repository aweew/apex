# 回测任务用户隔离实施计划

1. 在服务测试中模拟当前登录用户，覆盖任务写入、列表过滤、绩效榜过滤和越权读取。
2. 为 `BacktestJob` 增加 `userId` 字段，并在普通回测、组合回测落库时赋值。
3. 在 `BacktestServiceImpl` 增加统一的任务归属查询，替换无条件主键查询。
4. 在成交和净值读取前执行归属校验，使月度收益、压力测试、成交期望和导出自动继承边界。
5. 新增 `32_backtest_user_isolation.sql`，迁移存量数据并创建用户查询索引。
6. 先跑失败测试确认测试有效，再完成实现并执行聚焦、全量和差异检查。
