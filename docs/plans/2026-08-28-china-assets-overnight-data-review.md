# 隔夜中国资产数据补全审查

## 结论

核心链路通过审查。纳斯达克中国金龙指数保持独立字段，不改变原有三大美股指数口径；中概代表股保留完整报价列表，同时复用主题统计；A50 使用实际可返回数据的 `hf_CHA50CFD` 合约。

## 已修复问题

- 移动端布局测试原先断言了不存在的 `small.china-asset-time` 选择器，已改为真实承载元数据的 `small.china-asset-meta`。
- 腾讯和新浪行情在源时间为空或格式异常时原先仍会返回报价，可能造成无时间价格进入晨报。现已改为拒绝该条报价，并由既有完整性判断降级为 `YELLOW` 和“暂未获取”。

## 剩余风险

- 新浪 A50 公共端点在当前运行环境下仅 HTTP 可稳定访问，存在传输完整性风险。请求不携带认证信息或用户数据，但报价只能作为盘前外部参考，不能视为授权、可校验的交易级行情。后续应替换为支持 TLS 的授权数据源。
- 当前浏览器没有 Apex 登录态，无法完成真实填充数据下的桌面和移动端目测验收。本次仅完成静态布局契约测试和生产构建验证。

## 验证

- 后端受影响测试：58 个通过，0 失败。
- 前端晨报布局测试：30 个通过，0 失败。
- 前端全量测试：423 个通过，0 失败。
- 前端生产构建：通过，仅保留项目既有的大分块告警。
- `git diff --check`：通过。
