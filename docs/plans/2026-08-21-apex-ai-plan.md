# Apex AI 小灵实施计划

1. 新增请求、上下文、分析结果和归因项 DTO，以及分析类型枚举。
2. 新增 `IApexAiAnalystService` 与实现，完成组合归因、策略诊断、通用问答和 Kimi 安全增强。
3. 新增 `ApexAiController`，提供上下文和分析接口。
4. 新增后端单元测试，覆盖组合归因对账、策略证据和无模型降级。
5. 新增前端 API、`ApexAiView.vue`、路由、菜单和移动端标题。
6. 新增前端契约测试，覆盖页面结构、响应式布局、导航和请求状态。
7. 运行后端定向/全量测试、前端测试与构建，完成浏览器几何和视觉验收。
