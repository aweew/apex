# 历史回测股票池时间点实施计划

1. 先测试历史快照查询条件和刷新写入的数据截止日。
2. 扩展股票池实体、服务接口和实现。
3. 新增独立 SQL 迁移并回填存量快照。
4. 测试批量与组合回测按开始日取快照且不回退最新批次。
5. 测试快照写入、默认自选、按批次和按日期查询均按当前用户隔离。
6. 测试批次号不可碰撞，并让决策流程读取本次刷新批次。
7. 执行股票池、回测和后端全量测试。
