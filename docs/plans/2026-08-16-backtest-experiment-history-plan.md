# 回测实验历史实施计划

1. 为保存、用户过滤、详情解析和越权删除编写失败测试。
2. 新增实验实体、列表响应、详情响应、Mapper 和服务接口。
3. 实现当前用户实验的自动保存、限量列表、详情和逻辑删除。
4. 将滚动评估的实际默认值和 S1/S2/S3 可执行参数回填为可复跑请求，保存后返回实验 ID。
5. 新增控制器 API 和 `34_backtest_experiment.sql`，表与字段补充 MySQL `COMMENT`。
6. 为前端历史 API、参数恢复、策略快照匹配、两条对比和 390px 布局编写失败测试。
7. 在策略实验室接入历史列表、详情查看、按原配置复跑、删除和并排对比。
8. 执行聚焦测试、后端与前端全量测试、Node 20 生产构建和 `git diff --check`。
9. 审查用户隔离、JSON 快照、加载状态、移动端溢出和数据库迁移边界。
