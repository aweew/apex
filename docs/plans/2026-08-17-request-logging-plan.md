# 全局接口请求日志实现计划

1. 为 `RequestLogFilter` 编写测试，覆盖 traceId、手机号、参数脱敏、请求体复读、日志级别和 MDC 清理。
2. 实现统一请求日志过滤器及安全的可重复读请求包装。
3. 登录成功后将手机号写入 Sa-Token 会话，存量会话由过滤器按需补齐。
4. 让全局异常处理器标记 WARN/ERROR 请求结果。
5. 移除旧日志拦截器注册并调整 Logback MDC 字段、文件过滤和无效字符。
6. 运行定向测试、后端回归测试、编译和 `git diff --check`，再进行代码审查。
